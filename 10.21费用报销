 
 所有人先进行正常的按照上级查找
 //然后存在找不到的情况，在进行相应的判断（存在3种特殊情况 1.无总监经理（部门老大）2.5个特殊部门 3.总经理）
 if(获取到的审批人为空){
	//判断是否是总监
	if（=总监）{
		//判断是否是那5个部门
		if（=那5个部门）{
			//到副总
		} else {
		   //到会计
		}
	} else if（=经理）{
		//判断是否是那5个部门
		if（=那5个部门）{
			//到副总
		} else {
		   //到会计
		}
	} else { //总经理级别的了
		直接到会计
	}
 }
 
 

